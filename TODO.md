- [ ] Locate existing login/register/about pages or routing approach in the repo.
- [ ] Update `src/landing.jsx` links to point to real routes/components.
- [ ] If no router exists, implement a minimal router in `src/main.jsx` using React Router and create placeholder pages for /login, /register, /about, /features, /security, /faq.
- [ ] Ensure `npm run build` passes.

